# Decisions

- Prepare `v0.1.0` as the first release because both package manifests already declare version `0.1.0` and no tags exist.
- Date the release `2026-08-24` and add first-release GitHub changelog links.
- Commit all current requested changes in the release commit.
- Do not create the tag, push, or GitHub release during preparation.
