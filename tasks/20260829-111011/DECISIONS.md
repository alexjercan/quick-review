# Decisions

- Release version `0.2.0`.
- This is a pre-1.0 minor release because it adds the Claude Code adapter and the project graph, and replaces the linear walkthrough host tools.
- Mark the host-tool replacement as breaking in the changelog.
- Keep graph artifact, state, and completion versions at their implemented values. They are new contracts rather than changes to a released graph contract.
